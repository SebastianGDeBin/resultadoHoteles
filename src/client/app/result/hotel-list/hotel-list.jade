ul.ulHotels
    li.hotels(ng-repeat="hotel in $ctrl.hotels | filterSearch:($ctrl.filters.nameSearched) | filterStars:($ctrl.filters.stars) | filterPrice:($ctrl.filters.price) track by $index")
        hotel-item(hotel='hotel')